CREATE TABLE IF NOT EXISTS forms(elid INTEGER PRIMARY KEY,fid TEXT NOT NULL REFERENCES form_register(fid), typ TEXT NOT NULL,req BOOLEAN DEFAULT false,field_name VARCHAR NOT NULL,question TEXT NOT NULL,limited BOOLEAN DEFAULT false,limit_val INTEGER DEFAULT 0);